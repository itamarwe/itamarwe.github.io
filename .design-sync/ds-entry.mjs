// Tokens-only design-system entry for design-sync: this repo is a website,
// not a component package, so the sync ships styling (tokens, fonts, the
// compiled site stylesheet) with an intentionally empty JS bundle.
export {};
